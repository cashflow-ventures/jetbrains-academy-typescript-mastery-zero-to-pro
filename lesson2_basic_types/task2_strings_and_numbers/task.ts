// TODO: Implement the formatPrice function
// It should take a price (number) and currency (string),
// and return the currency symbol followed by the price with 2 decimal places.
// Example: formatPrice(29.9, "$") => "$29.90"
export function formatPrice(price: number, currency: string): string {
    // Write your solution here
    return "";
}

// TODO: Implement the initials function
// It should take firstName (string) and lastName (string),
// and return the first character of each in uppercase, separated by a dot.
// Example: initials("john", "doe") => "J.D"
export function initials(firstName: string, lastName: string): string {
    // Write your solution here
    return "";
}

// TODO: Implement the isEven function
// It should take a value (number) and return true if even, false otherwise.
export function isEven(value: number): boolean {
    // Write your solution here
    return false;
}

// TODO: Implement the celsiusToFahrenheit function
// It should take celsius (number) and return the Fahrenheit equivalent.
// Formula: F = C * 9/5 + 32
export function celsiusToFahrenheit(celsius: number): number {
    // Write your solution here
    return 0;
}
