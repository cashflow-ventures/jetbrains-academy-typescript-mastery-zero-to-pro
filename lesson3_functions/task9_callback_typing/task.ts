// TODO: Implement the applyToEach function
// It should take an array of numbers and a callback (value: number) => number.
// Return a new array where each element is the result of calling the callback.
export function applyToEach(
    values: number[],
    callback: (value: number) => number
): number[] {
    // Write your solution here
    return [];
}

// TODO: Implement the filterStrings function
// It should take an array of strings and a predicate (str: string) => boolean.
// Return a new array containing only strings where the predicate returns true.
export function filterStrings(
    values: string[],
    predicate: (str: string) => boolean
): string[] {
    // Write your solution here
    return [];
}

// TODO: Implement the createMultiplier function
// It should take a factor (number) and return a function (value: number) => number
// that multiplies its argument by the factor.
export function createMultiplier(factor: number): (value: number) => number {
    // Write your solution here
    return () => 0;
}

// TODO: Implement the applyTwice function
// It should take a value (string) and a transform (input: string) => string.
// Apply the transform to the value twice and return the final result.
// Example: applyTwice("hi", s => s + "!") => "hi!!"
export function applyTwice(
    value: string,
    transform: (input: string) => string
): string {
    // Write your solution here
    return "";
}
