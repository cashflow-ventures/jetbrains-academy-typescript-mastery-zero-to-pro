// TODO: Implement the sumAll function
// It should accept any number of numbers using a rest parameter
// and return their sum. If no arguments are passed, return 0.
export function sumAll(...numbers: number[]): number {
    // Write your solution here
    return 0;
}

// TODO: Implement the joinStrings function
// It should take a separator (string) and any number of parts (string rest param).
// Return all parts joined by the separator.
// Example: joinStrings("-", "a", "b", "c") => "a-b-c"
export function joinStrings(separator: string, ...parts: string[]): string {
    // Write your solution here
    return "";
}

// TODO: Implement the mergeObjects function
// It should accept any number of objects (Record<string, unknown> rest param)
// and return a single object with all properties merged (later objects override earlier ones).
// Use Object.assign or spread syntax.
export function mergeObjects(...objects: Record<string, unknown>[]): Record<string, unknown> {
    // Write your solution here
    return {};
}
