# Quiz: Declaration Files

Select **all** correct statements about declaration files, the `declare` keyword,
and declaration merging in TypeScript.

## Which of the following statements are true?

Think about how `.d.ts` files work, what `declare` means, which declarations can
merge, and where triple-slash directives must be placed.

<div class="hint">
Consider the differences between `interface` and `type` when it comes to merging.
Remember that `.d.ts` files are purely for type information — they never contain
executable code. And think about the strict placement rules for triple-slash directives.
</div>
