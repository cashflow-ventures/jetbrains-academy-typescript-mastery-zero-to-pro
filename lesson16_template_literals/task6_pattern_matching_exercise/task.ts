// TODO: Implement parseRoute
// Parses "METHOD /path" into { method, path }
// Returns null if the format doesn't match
export function parseRoute(route: string): { method: string; path: string } | null {
    // Write your solution here
    return { method: "TODO", path: "TODO" };
}

// TODO: Implement extractParams
// Extracts route parameter names from a path
// e.g., "/users/:id/posts/:postId" → ["id", "postId"]
export function extractParams(path: string): string[] {
    // Write your solution here
    return ["TODO"];
}

// TODO: Implement splitString
// Splits a string by the given delimiter
// e.g., splitString("a-b-c", "-") → ["a", "b", "c"]
export function splitString(str: string, delimiter: string): string[] {
    // Write your solution here
    return ["TODO"];
}
