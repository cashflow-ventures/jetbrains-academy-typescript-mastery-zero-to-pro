# Quiz: Advanced Generics

Test your understanding of multiple type parameters, `keyof`, indexed access types,
generic inference, and variance.

## Which statement about `keyof`, indexed access types, and advanced generics is correct?

Consider how `keyof T`, `T[K]`, type argument inference, and variance annotations work
together in TypeScript's type system.

<div class="hint">
Think about what `keyof T` produces (a union of literal types, not a runtime value),
how `T[K]` resolves to a specific property type, and whether variance annotations
and explicit type arguments are always required.
</div>
